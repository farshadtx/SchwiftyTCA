query GetCharacterQuery($id: ID!) {
    character(id: $id) {
        id
        name
        status
        species
        gender
        origin {
            name
        }
        location {
            name
        }
        episode {
            id
            name
            air_date
            episode
        }
        image
        created
    }
}
